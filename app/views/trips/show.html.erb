<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @trip.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @trip.description %>
</p>


<%= link_to 'New Step', new_trip_step_path(@trip) %>

<table>
  <thead>
    <tr>
      <th>Location</th>
      <th>Lon</th>
      <th>Lat</th>
      <th>Arrive on</th>
      <th>Stay</th>
      <th>Weather</th>
      <th>T max °C</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @trip.steps.each do |step| %>
      <tr>
        <td><%= link_to step.location, [@trip, step] %></td>
        <td><%= step.lon %></td>
        <td><%= step.lat %></td>
        <td><%= step.arrive_on %></td>
        <td><%= step.stay %></td>
        <td><%= step.forecasts.order('created_at desc').first.description %></td>
        <td><%= step.forecasts.order('created_at desc').first.temp_day %></td>
        <td><%= link_to 'Edit', edit_trip_step_path(@trip, step) %></td>
        
      </tr>
    <% end %>
  </tbody>
</table>


<%= link_to 'Edit', edit_trip_path(@trip) %> |
<%= link_to 'Back', trips_path %>
